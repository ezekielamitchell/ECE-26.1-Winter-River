# Sample Environment Variables — Native systemd install
# Copy this file to grafana/.env (git-ignored) and fill in real values.
# Then run: sudo ./scripts/setup_pi.sh

# ── InfluxDB 2 ────────────────────────────────────────────────────────────────
INFLUXDB_ADMIN_USER=influx-admin
INFLUXDB_ADMIN_PASSWORD=changeme_strong_influx_password
# API token: any string ≥ 32 characters, no spaces
INFLUXDB_ADMIN_TOKEN=changeme_influx_token_at_least_32_chars_long

# ── Grafana ───────────────────────────────────────────────────────────────────
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=changeme_strong_grafana_password

# Instructions:
# 1. Copy this file:  cp grafana/.env.sample grafana/.env
# 2. Replace every "changeme_*" value with a strong, unique secret
# 3. Never commit grafana/.env to version control
# 4. Re-run setup_pi.sh after changing values to push them to the services
