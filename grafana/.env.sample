# Sample Environment Variables for Grafana Stack
# Copy this file to .env (which is git-ignored) and customize

# InfluxDB Configuration
INFLUXDB_DB=mqtt_metrics
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=changeme_strong_password
INFLUXDB_USER=grafana
INFLUXDB_USER_PASSWORD=changeme_grafana_password

# Grafana Configuration
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=changeme_admin_password
GF_INSTALL_PLUGINS=

# MQTT Broker Connection
MQTT_BROKER_HOST=192.168.1.100  # IP of your Raspberry Pi
MQTT_BROKER_PORT=1883

# Instructions:
# 1. Copy this file to grafana/.env
# 2. Update all passwords with strong, unique values
# 3. Set MQTT_BROKER_HOST to your Raspberry Pi's IP address
# 4. Never commit .env to version control
